# 기본 정보
- 이름(영문) : Orbital Forest
- 이름(국문) : 궤도의 숲
- 제작자 : Luminon Canoness
- 패키지명 : space.byeolvit.of
# 기술 사양
- Jetpack Compose를 통한 UI 구현.
- Material 3 Expressive에 기반을 둔 커스텀 UI로 제공된 화면 구현.
- 제공한 Figma 문서를 토대로 Theme를 생성하고 활용하는 방식으로 UI를 구현해야 한다.
- Android에서 기본적으로 제공하는 접근성 옵션을 만족해야 함.
- 위의 항목을 만족하는 최소 지원 버전은 자유롭게 설정.
- 모든 UI 화면은 `Pretendard Variable` 서체를 사용한다. 제공이 필요하면 작업시 요청하라.
- 모든 UI 텍스트는 `word-break : keep-all`에 상응하는 대응을 한다. (단어 줄바꿈)
# 기획 설명
- 요약 : Markdown File(`.md`) 파일에 내용을 저장하는 To-Do 체크리스트 앱.
- 기본 구조 : 내부 저장소에 특정 폴더를 선택한 후, 하위에 앱이 사용하는 폴더를 만든다. 그리고 체크리스트를 저장할 `.md` 파일을 만들고 앱에서 체크리스트 내용을 보여주고 추가할 수 있도록 한다. `.md` 파일은 앱에서 추가로 생성할 수 있고 삭제할 수 있다.
## 상세 설명
- 모든 설명에는 화면 번호가 작성되어 있다. UI 디자인은 각 화면 번호에 상응하는 Figma 프레임을 제공한다. 작업 도중 화면 정보가 필요하다면 화면 번호를 참고해 요청하라.
- 마크다운 파일은 업데이트가 될 때 마다 저장하라. (ex. 체크리스트 항목 생성, 체크리스트 삭제, 파일이름 변경)
### Launch Screen (LNC)
- 시작 화면 (LNC_01)
	- 간단한 브랜드 일러스트가 표시된다.
	- '시작하기' 버튼이 있다. 시작하기 버튼을 누르면 'LNC_02' 화면으로 변경된다.
		- 액티비티가 전환되는 것이 아니라, 부드러운 Fade 효과로 내용만 전환된다.
	- 아래에는 AI가 사용되었음을 알리는 안내가 있다. ('본 어플리케이션은 개발 일부에 AI를 활용했습니다. 사용 이전에 AI Footprint 보고서로 내용을 검토할 수 있습니다.')
		- 'AI Footprint 보고서'에는 제공하는 링크를 삽입해야 한다.
- 폴더 선택 화면 (LNC_02)
	- 브랜드 일러스트는 유지되고, '시작하기'와 '안내'가 사라지고 '저장 위치 설정' 메뉴가 표시된다.
	- 메뉴를 누르면 Android 시스템을 통해 앱이 사용할 폴더를 선택하게 된다.
### Home (HME)
- Empty 화면 (HME_01)
	- 폴더 선택 이후 기본 문서(`내 할일.md`)가 만들어진다.
	- App Bar에는 문서 이름(내 할일.md)와 문서 정보 버튼(HME_13 표시)이 있다.
		- 문서 이름에서 확장자(`.md`)는 박스에 싸여 구분되도록 표시한다.
	- 하단에는 Tool Bar와 FAB가 있다.
		- Tool Bar에는 문서 생성(MDO_01 표시), 문서 선택(SED_01 이동), 설정(STN_01 이동) 버튼이 있다.
		- FAB는 '체크리스트 추가'(HME_10 표시) 버튼이다.
	- 화면 가운데에는 Empty View 콘텐츠가 있다.
		- 콘텐츠는 Empty View를 나타내는 이미지와 '새로운 할일을 추가해보세요'라는 텍스트로 구성된다.
	- md 문서까지 없는 화면은 'HME_15' 화면을 보여줘야 한다.
- 기본 화면 (HME_02)
	- HME_01의 화면에 Empty View 콘텐츠가 사라지고 체크리스트 항목이 표시된다.
	- 체크리스트는 다음과 같이 구성된다.
		- 체크한 체크리스트는 배경색이 강조된다.
		- 체크리스트는 들여쓰기로 두 단계까지 보여진다.(HME_04) 두 단계 이후로는 오류(HME_05)를 표시하며 보여주지 않는다.
		- 체크리스트 내부는 기본적인 Markdown 서식을 지원한다 (볼드, 기울기, 취소선)
		- 앱에서는 체크리스트 외의 내용을 작성할 수 없지만, 만약 문서에 체크리스트 외의 내용이 있다면 체크리스트를 끊고 메모 형식으로 내용을 보여준다. 
			- 기본적인 Markdown 서식(볼드, 기울기, 취소선, 줄바꿈)만 지원하고 제목, 표, 인용과 같은 복잡한 서식은 배제한다.
	- 체크리스트를 스크롤할 경우 App Bar, Tool Bar가 숨겨지고, FAB만 표시된다. (HME_03)
		- 다시 위로 스크롤할 경우 복구된다.
		- 화면 상단과 하단은 Fade 처리를 한다.
- 체크리스트 메뉴 (HME_06)
	- 체크리스트 항목을 길게 누르면 메뉴가 표시된다. 
		- 메뉴는 항목 미리보기, 복사(HME_08), 삭제(HME_07), 수정(HME_09A) 하위 항목(HME_09)로 이루어진다.
			- '복사'를 누르면 클립보드에 복사하고 Snackbar를 표시한다.(HME_08)
			- '삭제'를 누르면 항목을 지우고 삭제되었다는 Snackbar를 표시한다.(HME_07)
				- '실행 취소' 버튼을 누르면 다시 지운 항목을 복구한다.
			- '수정'을 누르면 '수정중' 텍스트와 현재 수정중인 항목이 있는 배너가 달린 작성 화면이 표시된다. (HME_09A)
				- 수정 이후 '수정되었습니다.' 라는 Snackbar를 표시한다.
			- '하위 항목'을 누르면 하단에 '하위 항목 작성중'텍스트와 상위 항목 이름이 있는 배너가 달린 작성 화면이 표시된다. (HME_09)
				- 하위 항목 버튼은 하위 체크리스트 메뉴에서는 비활성화된다.
- 작성 화면 (HME_10)
	- FAB를 누르면 작성 화면으로 전환된다.
		- 가능할 경우, FAB가 작성 필드가 되는 애니메이션을 추가한다.
		- 키보드를 자동으로 표시한다.
		- 작성 전에는 '작성해주시길 기다리고 있어요...' 문구를 Placeholder로 표시한다.
		- 키보드의 엔터는 줄바꿈으로 동작하며, 텍스트 필드는 최대 10줄까지 표시하고 스크롤 처리한다. (HME_12)
		- '+' 버튼을 누르면 항목이 추가된다.
- 문서 메뉴 화면 (HME_13)
	- App Bar의 문서 메뉴 버튼을 누르면 화면 위에 문서 메뉴 Sheet가 표시된다.
		- 문서 메뉴 시트는 문서 이름, 문서 요약, 삭제 버튼(HME_14), 이름 변경(HME_16)으로 구성된다.
		- 문서 이름은 App Bar의 이름과 동일한 서식으로 표시한다.
		- 문서 요약은 다음과 같이 구성된다.
			- 전체 체크리스트 항목 개수
			- 완료한 체크리스트 항목 개수
		- 삭제 버튼을 누르면 경고 다이얼로그(HME_14)가 표시되고, 삭제를 누르면 '문서를 삭제했습니다.' Snack Bar를 표시하며 남아 있는 폴더 내 문서중 하나로 이동한다.
			- 만약 남아 있는 문서가 하나도 없는 경우 Empty View(HME_15)를 표시한다.
		- 이름 변경 버튼을 누르면 이름 수정 텍스트 필드가 있는 다이얼로그를 표시한다. (HME_16)
			- 이름이 변경되면 Sheet 위에 '이름이 변경되었습니다.' SnackBar를 표시한다.
### Make Document (MDO)
- 새 문서 만들기 (MDO_01)
	- Tool Bar의 문서 생성 버튼을 누르면 '새 문서 만들기' 다이얼로그가 표시된다.
		- 문서 이름을 정하는 텍스트 필드가 있고 작성한 이름으로 새 문서를 만든다.
			- 문서 이름 기본값은 '내 할일 n'이다.
				- `n` 은 기존에 `내 할일.md`가 있는 경우, 겹치는 파일이 없을 때까지 +1 해서 만든다.
					- ex: `내 할일.md`, `내 할일 2.md` `내 할일 3.md`가 있는 경우, 기본값은 `내 할일 4.md`가 된다.
	- 문서를 만들면 '새 문서를 생성했어요.' SnackBar를 표시하고 해당 문서로 이동한다. (MDO_02)
### Select Document (SED)
- 문서 선택 (SED_01)
	- Tool Bar의 문서 선택 버튼을 누르면 문서 선택 화면으로 이동된다.
		- 최상단에는 '같은 폴더에 저장되어 있는 .md 문서가 모두 표시됩니다.'라는 안내 배너가 표시된다.
		- 배너 아래에 폴더 내에 있는 모든 `.md`문서를 목록으로 보여준다.
			- 지금 선택된 문서는 강조 표시한다.
			- `.md` 문서 외 파일을 표시하지 않는다.
		- 목록에 있는 문서를 길게 누를 경우, 문서 메뉴 화면(HME_13)을 표시한다.
### 설정 (STN)
- 설정 화면 (STN_01)
	- 설정 화면은 다음과 같이 구성된다.
	- 브랜딩 콘텐츠
		- 브랜드 일러스트와 로고, 소개 텍스트로 구성된다.
	- 설정 메뉴
		- 저장 위치 변경 : 누르면 초기 설정시 실행한 것과 동일하게 사용할 폴더를 다시 설정한다.
		- 완료한 설정 숨기기(토글, 기본값 OFF) : 토글을 켜면 완료한 항목을 목록에서 숨긴다.
		- 체크리스트 이외 항목 숨기기(토글, 기본값 OFF) : 토글을 켜면 체크리스트 외의 내용을 숨긴다.
		- 하위 항목 상호작용(토글, 기본값 ON) : 토글을 켜면 하위 항목을 모두 체크하면 상위 항목도 체크한다. 반대로, 하나라도 체크를 해제하면 상위 항목도 체크를 해제한다.
	- 애플리케이션 정보
		- 버전 : 애플리케이션 버전을 표시한다.
		- 디자인 : 디자이너를 표시한다. (Luminon Canoness)
		- AI FootPrint : AI가 사용된 부분을 정리한 URL을 연다.
